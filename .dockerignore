.git
.claude
__pycache__
*.pyc
.env
# The DB comes in via bind mount from the host (see bugbot.container); a copy baked into
# the image would just be dead weight that grows with every build.
features/sql_db/bugbot.db
