.env
bugbot.db

# Output of chatlog.py - viewer chat does not belong in the repository
chat-archive/
__pycache__/
*.pyc

# Tooling, not project (.dockerignore keeps these out too)
.claude/
.venv/
venv/
